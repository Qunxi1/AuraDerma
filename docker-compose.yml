services:
  qdrant:
    image: qdrant/qdrant:v1.11.5
    container_name: auraderma-qdrant
    ports:
      - "6388:6333"  # 使用 6388 避开 Windows 端口保留范围 (6283-6382)
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
